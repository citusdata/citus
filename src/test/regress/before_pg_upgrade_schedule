test: upgrade_basic_before
test: upgrade_type_before
test: upgrade_ref2ref_before

# Run after tests to confirm that output is the same before as after
test: upgrade_basic_after
test: upgrade_type_after
test: upgrade_ref2ref_after
